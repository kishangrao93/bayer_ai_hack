# =============================================================================
# Lambda runtime dependencies — used by the Dockerfile.
#
# Differences from requirements.txt (local/dev):
#   - sentence-transformers REMOVED: not used at Lambda runtime; pulls in
#     scikit-learn which requires numpy>=2 built from source (needs GCC>=9.3,
#     but the Lambda AL2 base image only has GCC 7.3.1).
#   - rank_bm25 REMOVED: not used at Lambda runtime.
#   - numpy pinned to <2.0 so pip picks a pre-built manylinux wheel instead
#     of attempting a source build.
# =============================================================================

# AWS SDK
boto3>=1.34,<2.0
botocore>=1.34,<2.0

# LangChain core + integrations
langchain>=0.3.0,<0.4
langchain-core>=0.3.0,<0.4
langchain-openai>=0.2.0,<0.3
langchain-aws>=0.2.0,<0.3        # kept for Bedrock KB retrieval (boto3 wrapper)
langchain-community>=0.3.0,<0.4

# OpenAI SDK (required by langchain-openai)
openai>=1.40.0,<2.0

# LangGraph (StateGraph orchestration)
langgraph>=0.2.0,<0.3

# LangSmith tracing (optional but imported)
langsmith>=0.1.0,<0.3

# Typing backport (Python 3.9+ Lambda runtime)
typing-extensions>=4.9,<5.0

# Numeric — pin below 2.0 to guarantee a pre-built wheel on AL2 / GCC 7
numpy>=1.26,<2.0
